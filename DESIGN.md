# Design System — تقدير

## Visual Theme

مشهد: **طاولة تحليل**. أسطح داكنة صلبة، خطوط شعرية بيضاء شفافة، أرقام جدولية واضحة.
لا زجاج ولا blur ولا توهج ولا صور لاعبين ولا شريط متحرك. اللون يحمل معنى فقط:
1X2 والدوريات. كل ما عداه رمادي مائل للأزرق.

المرجع الذهني: صفحات بيانات Opta / FiveThirtyEight، بالعربية وبكثافة أعلى.

## Anti-patterns

ممنوعة في الكود لا في النية فقط:

- `backdrop-filter` أو زجاجية بأي شكل
- تدرّج كسطح (التدرّج مقبول داخل علامة بيانات فقط، مثل سلّم خريطة النتائج)
- توهج، ظل ملوّن، ظل للزينة
- صورة فوتوغرافية أو رسم زخرفي
- شريط متحرك أو أي حركة تلقائية مستمرة
- حركات دخول متسلسلة (`animation-delay` تصاعدي)
- كتلة لون مشبعة كبيرة للتنقّل
- رقم عملاق كزينة (hero metric)

## Color (OKLCH)

| توكن | القيمة | الاستعمال |
|------|--------|-----------|
| `--bg` | `oklch(0.16 0.011 250)` | خلفية الصفحة، صلبة |
| `--surface` | `oklch(0.202 0.013 250)` | البطاقات |
| `--panel` | `oklch(0.245 0.015 250)` | تمرير، رؤوس جداول، سكة اليوم |
| `--raised` | `oklch(0.285 0.016 250)` | طبقة ثالثة نادرة |
| `--ink` | `oklch(0.968 0.004 250)` | نص أساسي |
| `--muted` | `oklch(0.735 0.011 250)` | نص ثانوي |
| `--faint` | `oklch(0.62 0.012 250)` | نص ثالثي، لا يحمل معنى وحده |
| `--line` | `oklch(1 0 0 / 0.085)` | خط شعري |
| `--line-strong` | `oklch(1 0 0 / 0.17)` | خط عند التمرير |
| `--accent` | `oklch(0.7 0.115 245)` | لهجة واحدة: روابط، تركيز، نشط |
| `--home` | `oklch(0.715 0.115 248)` | 1 مضيف |
| `--draw` | `oklch(0.79 0.1 85)` | X تعادل |
| `--away` | `oklch(0.7 0.125 33)` | 2 ضيف |

ألوان الدوريات (`[data-league]`) تظهر **فقط** كنقطة 0.4rem أو خط بدء 2px — لا كخلفية.

اللون لا يحمل معنى وحده أبداً: رمز `1` / `X` / `2` أو الرقم حاضر دائماً بجانبه.

## Typography

عائلة واحدة: **IBM Plex Sans Arabic** (400/500/600). لا خط عرض ثانٍ.

| صنف | المقاس | الاستعمال |
|-----|--------|-----------|
| `.type-display` | 1.5–1.75rem / 600 | عنوان صفحة محورية |
| `.type-page` | 1.25–1.4rem / 600 | عنوان صفحة |
| `.type-section` | 0.9375rem / 600 | عنوان قسم |
| `.type-label` | 0.6875rem / 500 | تسمية حقل |
| `.type-figure` | 1.5rem / 600 | رقم للقراءة السريعة |

`.tabular` إلزامي على كل رقم: نتائج، نسب، تواريخ، Elo، خلايا الجداول.

## Layout

- شريط علوي لاصق صلب + محتوى بعرض `--content-max` = 1080px
- فجوة أقسام: `space-y-6` · حشو بطاقة: `p-4 sm:p-5` · صف كثيف: `py-2.5`
- إيقاع المسافات مضاعفات 4px
- قائمة المباريات صفوف كثيفة، ليست شبكة بطاقات
- صفحة المباراة: مواجهة → إشارة 1X2 → تفكيك الإشارة → خريطة النتائج → أسواق → مصدر
- خصائص منطقية فقط (`ms/me/ps/pe/start/end`) — لا `left/right` إطلاقاً

## Components

`.card` · `.card-head` · `.match-row` · `.day-rail` · `.prob-track`/`.prob-fill` ·
`.pick-chip` · `.verdict-chip` · `.chip-filter` + `.chip-dot` · `.table-clean` ·
`.league-band` · `.league-row` · `.league-name-chip` · `.heatmap-cell`

الأوليات المشتركة في `src/components/ui.tsx`.

## Motion

`--motion-fast` 140ms · `--motion-base` 200ms · `--ease` = `cubic-bezier(0.2, 0, 0, 1)`.

مسموح شيئان فقط:

1. `.motion-colors` — انتقال لون/خلفية/حد لحالات hover و focus
2. `.is-inview` — كشف واحد لأشرطة البيانات عند أول ظهور

`prefers-reduced-motion: reduce` يوقف كل حركة وانتقال عبر قاعدة شاملة واحدة.

## Accessibility

- جداول حقيقية `<th scope="col">` · قوائم تعريف `<dl>/<dt>/<dd>`
- تركيز مرئي عالمي: `:focus-visible` بحد 2px بلون اللهجة
- كل أيقونة بلا نص لها `aria-label` · كل صورة زخرفية `alt=""` + `aria-hidden`
- أشرطة الاحتمال تُقرأ نصياً لا لوناً
